hostname=getHostname()

kill("hack.script")

threads = Math.floor(getServerMaxRam(hostname)/getScriptRam("hack.script"))
tprint(hostname," - ",threads)
if (threads > 0) {
    run("hack.script",  threads)    
}