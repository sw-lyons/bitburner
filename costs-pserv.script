for (i=2;i<21;++i){
	cost = getPurchasedServerCost(Math.pow(2,i))
	count = Math.floor(getServerMoneyAvailable("home")/cost)
	if (count <=25){
		break
	}
	tprint(Math.pow(2,i)+ "gb ",nFormat(cost,"$0.000a "),count)
	
}